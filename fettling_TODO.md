This is not important in the grand scheme of things, including finishing the dissertation.

It is, however, worthwhile fettling. Closing these gaps would make the cache state and downstream comparisons more scientifically sound.

- Cache `weighted_linear` learning and screening results for all nine datasets.
- Cache `moe` screening results for `oc20_dense_nhx`.
- After those reruns, re-check that every non-excluded method shares the same combined budget grid within each dataset.
